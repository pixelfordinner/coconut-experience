
varying vec2 vUv;
uniform float iGlobalTime;
//chunk(fog_pars_vertex);


void main() {
  vUv = uv;
  //chunk(begin_vertex);
	//chunk(project_vertex);
	//chunk(worldpos_vertex);

  //chunk(fog_vertex);




}
